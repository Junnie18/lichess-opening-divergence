# Lichess personal access token (required as of 2026 — the Opening Explorer
# API now requires OAuth authentication, see README "API access" section).
# Generate one for free at https://lichess.org/account/oauth/token
# No scopes need to be checked — an empty-scope token is sufficient.
LICHESS_TOKEN=
